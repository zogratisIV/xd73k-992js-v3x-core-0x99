name: Interactive SSH Terminal

on:
  workflow_dispatch:

jobs:
  ssh-session:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Upterm Session
        uses: owenthereal/action-upterm@v1
        with:
          limit-access-to-actor: false
